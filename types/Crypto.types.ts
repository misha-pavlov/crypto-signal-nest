export type Crypto = {
  id: string;
  icon: string;
  name: string;
  price: number;
  symbol: string;
};

export type ForecastType = {
  buyPercentage: number;
  sellPercentage: number;
  holdPercentage: number;
};
